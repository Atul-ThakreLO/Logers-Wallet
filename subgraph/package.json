{
  "name": "@logers-wallet/subgraph",
  "version": "0.0.1",
  "private": true,
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy logers-wallet",
    "clean": "rm -rf build generated"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.93.0",
    "@graphprotocol/graph-ts": "^0.36.0"
  }
}
